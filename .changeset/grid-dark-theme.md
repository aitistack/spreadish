---
'@spreadish/react': minor
---

Add light/dark/system theming for SpreadsheetGrid cells, headers, and borders via CSS tokens and an optional `theme` prop that follows host `data-theme` / `prefers-color-scheme`.
