---
'@spreadish/sometic': minor
---

Wire Phase 07 Sometic persistence. `@spreadish/sometic` now ships a real IndexedDB `StorageAdapter`, `createWorkbookSession` (persistent store + query cache + offline mutation queue + sync transport), and playground header save/offline status backed by autosaved IndexedDB snapshots.
