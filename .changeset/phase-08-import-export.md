---
'@spreadish/core': minor
---

Add Phase 08 import/export: validated workbook JSON round-trip with schema migration, CSV/TSV sheet export of the used range, delimited import planning, and an `importSheetGrid` command that applies a grid in one undoable step.
