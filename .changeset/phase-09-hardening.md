---
'@spreadish/react': patch
---

Phase 09 hardening: seeded property/fuzz helpers (`createSeededRng`, `runProperty`), grid `id` for skip-link focus targets, and playground/CI harnesses for benchmarks, bundle reporting, and dependency audit.
