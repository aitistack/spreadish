---
'@spreadish/sometic': minor
---

Add `createWorkbookWorkspace` for multi-workbook hosts: catalog persistence, legacy single-key migration, create/switch/rename/remove, and one active `WorkbookSession` at a time.
