# Basic gates for the development branch (faster feedback than full PR CI).
name: Development

on:
    push:
        branches: [development]
    pull_request:
        branches: [development]

concurrency:
    group: development-${{ github.workflow }}-${{ github.ref }}
    cancel-in-progress: true

jobs:
    basic:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v6

            - uses: oven-sh/setup-bun@v2
              with:
                  bun-version: latest

            - run: bun install --frozen-lockfile
            - run: bun run format:check
            - run: bun run lint
            - run: bun run typecheck
            # Workspace package.json exports resolve to dist/; build before Bun tests.
            - run: bun run build
            - run: bun run test:unit
            - run: bun run check:circular
